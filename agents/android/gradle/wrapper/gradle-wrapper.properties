# Illustrative only — the wrapper jar (gradle-wrapper.jar) itself is a binary fetched by
# `gradle wrapper`, which needs network + a real Gradle install; neither is available in
# the box this skeleton was authored in. This file documents the intended distribution.
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-8.9-bin.zip
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
