# Skeleton placeholder — no release-build minification rules needed yet (isMinifyEnabled
# is false in app/build.gradle.kts). Add kotlinx-serialization / Room keep rules here once
# this module actually builds and a release build is exercised.
